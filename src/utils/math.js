const privateValue = "This value is private to this module";

export function add(x, y) {
  return x + y;
}

export function subtract(x, y) {
  return x - y;
}

export function multiply(x, y) {
  return x * y;
}

export function square(x) {
  return x * x;
}
